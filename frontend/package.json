{
  "name": "iplus-desktop",
  "version": "0.4.0",
  "private": true,
  "description": "iPlus 데스크톱 셸 (Tauri). 프론트는 dist/ 정적 UI.",
  "scripts": {
    "tauri": "tauri",
    "dev": "tauri dev",
    "build": "tauri build"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2"
  }
}
